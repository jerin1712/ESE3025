#include<stdio.h>
#include<stdlib.h>
#include<string.h>
typedef struct person
{
char lastname[20];
char firstname[20];
char city[20];
}
person_t;
int compare(void *pa, void *pb)
{0
char *lna=((person_t*)pa)->lastname;
char *lnb=((person_t*)pb)->firstname;
return strcmp(lna,lnb);
}
int main(void)
{
person_t database[100];
char string1,string2,string3,a,b;
FILE *fp;
fp=fopen("stinghw.txt","r+w");
whlie((a=fgetc(fb))!=EOF);
{
printf("%c",a);}
size_t count=0;
whlie((fp,"%c %c #c" string1,string2,string3))
{
strcpy(database[count].lastname,string1);
strcpy(database[count].firstname,string2);
strcpy(database[count].city,string3);
++count;
}
for(i=o;i<count;i++)
{
printf("%c",database[i].lastname);
printf("%c",database[i].firstname);
printf("%c",database[i].city);
fclose(fp);
}
